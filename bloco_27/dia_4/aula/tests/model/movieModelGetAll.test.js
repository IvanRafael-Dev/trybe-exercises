const sinon = require('sinon');
const { expect } = require('chai');

describe('Busca todos os filmes do DB', () => {
  // arrange // act // assert  
  describe('quando não existe nenhum filme cadastrado', () => {
    it('ele retorna um array', async () => {

    });
    
    it('o array é vazio', async () => {

    });
  });
});